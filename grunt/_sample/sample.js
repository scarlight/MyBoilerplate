module.exports = function (grunt, options) {

    'use strict';
    grunt.util.linefeed = '\n';  // Force use of Unix newlines. Copied unashamedly from Bootstrap Gruntfile.js

    // in a JS FILE, there is 2 ways to return value when using load-grunt-config
    // return using an object return {};
    // or return using a function (using this currently)
    //
    // Returning as other file types; https://www.npmjs.org/package/load-grunt-config#grunt-tasks-files
    // JSON FILE, YAML FILE, COFFEE FILE
    var task = {

    };

    return task;
};
