---
#layout: default.hbs
#Doing prefix via object to avoid naming collision
yfm:
    slug: index
---
<!doctype html>
<html class="no-js" lang="en">
    <head>
    <title>{{title}}</title>
    {{> headMeta ~}}
    {{> headLink ~}}
    {{> headScript ~}}
    </head>
    <body>
        {{> bodyIEBrowserWarning ~}}
        <div id="browser-wrapper">
            <div id="main-wrapper">
                <div id="viewport-wrapper">
                    <div id="{{ settings.prefix ~}}-header-wrp">
                        <div id="{{ settings.prefix ~}}-header">
                            <div id="{{ settings.prefix ~}}-company-logo">
                                <h1>{{ settings.companyName ~}}<a title="{{ settings.companyName ~}}" rel="home" href="index.php"><span></span></a></h1>
                            </div>
                            <div class="clear"></div>
                        </div>
                        {{> mainMenu this ~}}
                    </div>
                    <div id="{{ settings.prefix ~}}-body-wrp">
                        <div id="{{ settings.prefix ~}}-body">
                            {{> bodySampleContent ~}}
                        </div>
                        <div class="clear"></div>
                    </div>
                </div>
                <!-- *remove comment -> Place footer inside or outside of viewport -->
                {{> bodyFooter ~}}
            </div>
        </div>
        {{> googleAnalytics ~}}
    </body>
</html>
